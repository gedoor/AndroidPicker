apply plugin: 'com.android.library'

ext {
    isLibrary = true
    pomArtifactId = "ImagePicker"
    pomDescription = "image picker for android, like qq image picker"
}

dependencies {
    api 'com.squareup.picasso:picasso:latest.release'
    api project(":library:Common")
}
